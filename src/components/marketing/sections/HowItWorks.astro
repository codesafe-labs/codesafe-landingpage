<section id="how" class="border-t border-border bg-white">
	<div class="mx-auto max-w-landing px-8 py-[88px]">
		<div class="flex max-w-[560px] flex-col gap-3">
			<span
				class="font-mono text-[13px] font-semibold tracking-[0.06em] text-green"
				>HOW IT WORKS</span
			>
			<h2
				class="m-0 font-display text-[38px] font-bold leading-[1.1] tracking-tight"
			>
				From scan to merged fix in three steps
			</h2>
		</div>
		<div
			class="mt-12 grid gap-6 md:grid-cols-2 lg:grid-cols-3"
		>
			<div
				class="flex flex-col gap-3 rounded-[14px] border border-border bg-cream p-7"
			>
				<span class="font-mono text-[13px] font-semibold text-green">01</span>
				<h3 class="m-0 text-[19px] font-semibold">Connect your repo</h3>
				<p class="m-0 text-[15px] leading-relaxed text-muted text-pretty">
					Install the GitHub app, or run <code
						class="rounded-[5px] bg-[#EEF1EC] px-1.5 py-0.5 font-mono text-[13px]"
						>npx codesafe scan</code
					> locally. Nothing to configure.
				</p>
			</div>
			<div
				class="flex flex-col gap-3 rounded-[14px] border border-border bg-cream p-7"
			>
				<span class="font-mono text-[13px] font-semibold text-green">02</span>
				<h3 class="m-0 text-[19px] font-semibold">Scan finds the real holes</h3>
				<p class="m-0 text-[15px] leading-relaxed text-muted text-pretty">
					Leaked keys, unprotected admin routes, missing auth, SQL injection:
					the mistakes AI codegen actually makes, explained in plain English.
				</p>
			</div>
			<div
				class="flex flex-col gap-3 rounded-[14px] border border-green-border bg-green-pale p-7 md:col-span-2 lg:col-span-1"
			>
				<span class="font-mono text-[13px] font-semibold text-green">03</span>
				<h3 class="m-0 text-[19px] font-semibold">Merge the fix PR</h3>
				<p class="m-0 text-[15px] leading-relaxed text-[#33443B] text-pretty">
					codesafe writes a minimal diff, re-scans it, and opens a pull request.
					You review, merge, and get back to shipping.
				</p>
			</div>
		</div>
	</div>
</section>
