<section id="pr" class="border-t border-border bg-white">
	<div
		class="mx-auto grid max-w-landing items-center gap-16 px-8 py-[88px] lg:grid-cols-[0.85fr_1.15fr]"
	>
		<div class="flex flex-col gap-4">
			<span
				class="font-mono text-[13px] font-semibold tracking-[0.06em] text-green"
				>THE PRODUCT IS THE PR</span
			>
			<h2
				class="m-0 font-display text-[38px] font-bold leading-[1.1] tracking-tight text-balance"
			>
				Other tools hand you a report. We hand you the fix.
			</h2>
			<p class="m-0 text-[16.5px] leading-relaxed text-muted text-pretty">
				Every finding comes as a minimal, reviewed diff with an explanation a
				non-security person can understand. If you can merge a pull request, you
				can secure your app.
			</p>
			<ul class="m-2 flex list-none flex-col gap-3 p-0 text-[15px] text-[#33443B]">
				<li class="flex items-baseline gap-2.5">
					<span class="font-bold text-green">✓</span> Minimal diffs, no drive-by
					refactors
				</li>
				<li class="flex items-baseline gap-2.5">
					<span class="font-bold text-green">✓</span> Every fix is re-scanned before
					the PR opens
				</li>
				<li class="flex items-baseline gap-2.5">
					<span class="font-bold text-green">✓</span> Plain-English "what could have
					happened" for each hole
				</li>
			</ul>
		</div>

		<div
			class="overflow-hidden rounded-[14px] border border-[#DDE1DB] bg-white shadow-[0_18px_48px_-28px_rgba(24,32,27,0.35)]"
		>
			<div
				class="flex flex-col gap-2 border-b border-[#EBEEE8] px-[22px] pb-3.5 pt-[18px]"
			>
				<div class="flex items-center gap-2.5">
					<span
						class="inline-flex items-center gap-1.5 rounded-full border border-green-border bg-green-pale px-[11px] py-1 text-[12.5px] font-semibold text-green-dark"
					>
						<svg width="12" height="12" viewBox="0 0 16 16" fill="#10805A">
							<path
								d="M7.177 3.073 9.573.677A.25.25 0 0 1 10 .854v4.792a.25.25 0 0 1-.427.177L7.177 3.427a.25.25 0 0 1 0-.354ZM3.75 2.5a.75.75 0 1 0 0 1.5.75.75 0 0 0 0-1.5Zm-2.25.75a2.25 2.25 0 1 1 3 2.122v5.256a2.251 2.251 0 1 1-1.5 0V5.372A2.25 2.25 0 0 1 1.5 3.25ZM11 2.5h-1V4h1a1 1 0 0 1 1 1v5.628a2.251 2.251 0 1 0 1.5 0V5A2.5 2.5 0 0 0 11 2.5Zm1 10.25a.75.75 0 1 1 1.5 0 .75.75 0 0 1-1.5 0ZM3.75 12a.75.75 0 1 0 0 1.5.75.75 0 0 0 0-1.5Z"
							></path>
						</svg>
						Open
					</span>
					<span class="text-[15px] font-semibold"
						>fix: require admin auth on /api/admin routes</span
					>
					<span class="text-sm font-medium text-subtle">#12</span>
				</div>
				<div class="text-[13px] text-subtle">
					<strong class="font-semibold text-muted">codesafe[bot]</strong> wants to
					merge 1 commit into <code
						class="rounded-[5px] bg-green-pale px-1.5 py-px font-mono text-xs text-green-dark"
						>main</code
					> from <code
						class="rounded-[5px] bg-green-pale px-1.5 py-px font-mono text-xs text-green-dark"
						>codesafe/fix-admin-auth</code
					>
				</div>
			</div>
			<div class="font-mono text-[12.5px] leading-[1.7]">
				<div
					class="border-b border-[#EBEEE8] bg-[#F6F8F4] px-[22px] py-2 font-semibold text-muted"
				>
					app/api/admin/route.ts
				</div>
				<div class="overflow-x-auto py-3">
					<div class="whitespace-pre px-[22px] text-subtle">
						export async function GET(req: Request) {'{'}
					</div>
					<div class="whitespace-pre bg-[#EAF6EE] px-[22px] text-[#116A44]">
						+ const session = await auth();
					</div>
					<div class="whitespace-pre bg-[#EAF6EE] px-[22px] text-[#116A44]">
						+ if (!session?.user?.isAdmin) {'{'}
					</div>
					<div class="whitespace-pre bg-[#EAF6EE] px-[22px] text-[#116A44]">
						+ return new Response("Unauthorized", {'{ status: 401 }'});
					</div>
					<div class="whitespace-pre bg-[#EAF6EE] px-[22px] text-[#116A44]">
						+ {'}'}
					</div>
					<div class="whitespace-pre px-[22px] text-subtle">
						const users = await db.query.users.findMany();
					</div>
					<div class="whitespace-pre px-[22px] text-subtle">
						return Response.json(users);
					</div>
				</div>
			</div>
			<div
				class="mx-[22px] mb-5 flex flex-col gap-1.5 rounded-[10px] border border-border bg-cream p-4"
			>
				<span
					class="text-xs font-bold tracking-[0.05em] text-green"
					>WHY THIS MATTERS</span
				>
				<p class="m-0 text-[13.5px] leading-[1.55] text-muted text-pretty">
					Anyone on the internet could list every user in your database, no login
					required. This adds an admin check before any data is returned.
				</p>
			</div>
		</div>
	</div>
</section>
