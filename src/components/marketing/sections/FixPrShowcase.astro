<section id="pr" class="crt-divider">
	<div
		class="mx-auto grid max-w-landing items-center gap-14 px-8 py-[72px] lg:grid-cols-[0.85fr_1.15fr]"
	>
		<div class="flex flex-col gap-[18px]">
			<span class="crt-section-label">═══ THE PRODUCT IS THE PR ═══</span>
			<h2
				class="m-0 text-[34px] font-bold leading-[1.15] text-crt-cream text-balance"
			>
				OTHER TOOLS HAND YOU A REPORT. WE HAND YOU THE FIX.
			</h2>
			<p class="m-0 text-[13.5px] leading-[1.8] text-crt-body text-pretty">
				Every finding comes as a minimal, reviewed diff with an explanation a
				non-security person can understand. If you can merge a pull request, you
				can secure your app.
			</p>
			<ul
				class="m-1 flex list-none flex-col gap-2.5 p-0 text-[13px] text-[#D8C7A8]"
			>
				<li class="flex gap-2.5">
					<span class="text-crt-green">✓</span> MINIMAL DIFFS, NO DRIVE-BY REFACTORS
				</li>
				<li class="flex gap-2.5">
					<span class="text-crt-green">✓</span> EVERY FIX RE-SCANNED BEFORE THE PR
					OPENS
				</li>
				<li class="flex gap-2.5">
					<span class="text-crt-green">✓</span> PLAIN-ENGLISH “WHAT COULD HAVE HAPPENED”
				</li>
			</ul>
		</div>

		<div
			class="crt-card overflow-hidden shadow-[0_0_60px_rgba(245,166,35,0.07)_inset]"
		>
			<div
				class="flex flex-col gap-2 border-b border-dashed border-crt-border px-[22px] pb-3.5 pt-4"
			>
				<div class="flex flex-wrap items-center gap-3">
					<span
						class="border border-crt-green px-2.5 py-[3px] text-[11px] font-bold tracking-[0.06em] text-crt-green"
						>◉ OPEN</span
					>
					<span class="min-w-0 text-[13.5px] font-semibold text-crt-cream"
						>fix: require admin auth on /api/admin routes</span
					>
					<span class="text-[12.5px] text-crt-muted">#12</span>
				</div>
				<div class="text-[11.5px] text-crt-muted">
					codesafe[bot] wants to merge 1 commit into <span class="text-crt-amber"
						>main</span
					> from <span class="text-crt-amber">codesafe/fix-admin-auth</span>
				</div>
			</div>
			<div class="text-[12.5px] leading-[1.85]">
				<div
					class="border-b border-dashed border-crt-border px-[22px] py-2 text-crt-muted"
				>
					── app/api/admin/route.ts
				</div>
				<div class="overflow-x-auto py-3">
					<div class="whitespace-pre px-[22px] text-crt-faint">
						export async function GET(req: Request) {'{'}
					</div>
					<div
						class="whitespace-pre bg-crt-diff-bg px-[22px] text-crt-green"
					>
						+ const session = await auth();
					</div>
					<div
						class="whitespace-pre bg-crt-diff-bg px-[22px] text-crt-green"
					>
						+ if (!session?.user?.isAdmin) {'{'}
					</div>
					<div
						class="whitespace-pre bg-crt-diff-bg px-[22px] text-crt-green"
					>
						+ return new Response("Unauthorized", {'{ status: 401 }'});
					</div>
					<div
						class="whitespace-pre bg-crt-diff-bg px-[22px] text-crt-green"
					>
						+ {'}'}
					</div>
					<div class="whitespace-pre px-[22px] text-crt-faint">
						const users = await db.query.users.findMany();
					</div>
					<div class="whitespace-pre px-[22px] text-crt-faint">
						return Response.json(users);
					</div>
				</div>
			</div>
			<div
				class="mx-[22px] mb-5 flex flex-col gap-1.5 border border-dashed border-crt-border-strong px-4 py-[13px]"
			>
				<span
					class="text-[11px] font-bold tracking-[0.1em] text-crt-amber"
					>WHY THIS MATTERS</span
				>
				<p class="m-0 text-[12.5px] leading-[1.7] text-crt-body text-pretty">
					Anyone on the internet could list every user in your database, no login
					required. This adds an admin check before any data is returned.
				</p>
			</div>
		</div>
	</div>
</section>
